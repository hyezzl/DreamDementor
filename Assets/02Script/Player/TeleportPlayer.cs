using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// 씬 이동 시 플레이어를 특정 위치로 이동
/// </summary>
public class TeleportPlayer : MonoBehaviour
{
    // 다음 씬에 텔레포트될 좌표
    public static Vector3 targetPos = Vector3.zero;

    // 아니면 SO로 관리하는건...? 
    // 우선 T -> H new Vector3(0.5f, 0f, 0f);
}
